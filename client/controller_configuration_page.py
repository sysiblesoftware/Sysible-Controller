# Superseded - the Sysible Controller Configuration page was merged
# into client/admin_configuration_page.py (AdminConfigurationPage),
# which now covers Controller Configuration, Administrators,
# Administrator Password Policy, and the Audit Log as one single
# stacked page. See client/home.py - the "Sysible Controller Settings" dashboard
# card opens that page directly; there is no separate Controller
# Configuration card anymore.
#
# Kept as an empty module (rather than deleted) only because nothing
# in this codebase currently imports it; if you're looking for the
# hostname/IP/port fields, they're in AdminConfigurationPage now.
