# Test-only dependencies (the API test-suite in tests/).
# Install alongside the runtime deps:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
httpx>=0.27          # Starlette TestClient transport
