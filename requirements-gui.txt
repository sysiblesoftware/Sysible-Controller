# Desktop GUI client (PySide6) dependencies — OPTIONAL.
#
# The controller itself (FastAPI backend + the browser-based web console) does
# NOT need any of these; only the native desktop GUI (`sysible_controller gui`)
# does. The installer installs this file best-effort and never fails the install
# if it can't, so a headless server or an ARM box (Raspberry Pi) installs the
# controller cleanly.
#
# Note: PySide6 has no pip wheels on some platforms (notably ARM / Raspberry Pi
# and brand-new Python versions). On those, install the GUI deps from your
# distro instead, e.g.:  sudo apt install python3-pyside6 python3-qtawesome
PySide6>=6.6
qtawesome>=1.3
pyte>=0.8        # VT emulation so vi/top/less render correctly in the SSH terminal
