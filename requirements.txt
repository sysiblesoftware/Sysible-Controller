# Backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
psutil>=5.9
paramiko>=3.4
python-multipart>=0.0.9
cryptography>=42.0

# Agent
requests>=2.31

# Desktop GUI client dependencies live in requirements-gui.txt — they are NOT
# required by the controller (backend + web console) and are intentionally
# optional. PySide6 in particular has no wheels on some platforms (e.g. ARM /
# Raspberry Pi), where the GUI uses the distro's PySide6 package instead. The
# installer installs them best-effort so a headless/ARM controller still works.
