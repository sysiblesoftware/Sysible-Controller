# Sysible Controller runtime dependencies — pinned for reproducible builds.
# These exact versions are what the test suite is validated against. Bump them
# deliberately (not implicitly at install time). The pip-audit gate + CycloneDX SBOM
# this calls for now ship as .github/workflows/security-scan.yml (weekly + on change)
# and scripts/security_scan.sh (run locally). For a fully hash-locked manifest regenerate
# with:  pip-compile --generate-hashes requirements.in  (deploy with --require-hashes)
# (a floating `>=` set previously resolved to whatever was newest on PyPI at
# install time — non-reproducible and hard to attest for change control).

# Backend
fastapi==0.139.0
uvicorn[standard]==0.50.2
pydantic==2.13.4
psutil==7.2.2
paramiko==5.0.0
python-multipart==0.0.32   # multipart/form-data (file upload endpoints)
itsdangerous==2.2.0        # Starlette SessionMiddleware (web console login cookie)
cryptography==49.0.0
tzdata==2026.3             # IANA tz database for zoneinfo — schedule times fire in the
                           # operator's timezone even on a slim container with no system tzdb

# Agent
requests==2.34.2
