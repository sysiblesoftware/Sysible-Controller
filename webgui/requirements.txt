# Sysible Web Console service (BFF). The host-management logic is reused
# from the repo's existing client/ package, so this only needs the web
# layer. The controller's own requirements (requests, etc.) must also be
# importable - run this service in the same environment as the controller,
# or `pip install -r ../requirements.txt` as well.
# Pinned for reproducible builds — keep in step with ../requirements.txt.
fastapi==0.139.0
uvicorn[standard]==0.50.2
itsdangerous==2.2.0          # required by Starlette SessionMiddleware
python-multipart==0.0.32     # form/file uploads
