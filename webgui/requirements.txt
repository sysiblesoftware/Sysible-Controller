# Sysible Web GUI service (BFF). The host-management logic is reused
# from the repo's existing client/ package, so this only needs the web
# layer. The controller's own requirements (requests, etc.) must also be
# importable - run this service in the same environment as the controller,
# or `pip install -r ../requirements.txt` as well.
fastapi>=0.110
uvicorn[standard]>=0.27
itsdangerous>=2.1          # required by Starlette SessionMiddleware
python-multipart>=0.0.9    # form/file uploads (later phases)
